---
description: 현재 변경사항에 대한 커밋 메시지 추천
---

`git status`와 `git diff` (스테이지된 것 + 안 된 것 모두)를 확인하고,
최근 `git log`를 살펴 이 레포의 커밋 컨벤션을 파악한 뒤,
그 스타일에 맞춰 커밋 메시지를 1~2개 추천해줘.

- 실제로 `git add`나 `git commit`은 하지 말고 **메시지만 제안**할 것.
- 변경 범위가 여러 주제로 나뉘면 분할 커밋 안도 함께 제시해도 됨.
- 추천 메시지는 코드 블록으로 감싸서 바로 복사 가능하게 출력할 것.
